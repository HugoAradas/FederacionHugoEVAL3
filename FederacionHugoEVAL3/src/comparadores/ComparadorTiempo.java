package comparadores;

import entidades.Tiempo;

public class ComparadorTiempo implements Comparable<Tiempo>{

	@Override
	public int compareTo(Tiempo o2) {
//		return this.getHoras().compareTo(o2.getHoras());
		return 0;
	}
}
